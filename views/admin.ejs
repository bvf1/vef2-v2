<%- include('header') -%>
<h1>Viðburða umsjón</h1>
<h2>Viðburðir á næstunni</h2>
<% if (events.length === 0) { %>
  <p>Engin event!</p>
<% } else { %>
  <ul>
    <% events.forEach((event) => { %>
      <li>
        <a href="/<%= event.slug %>"><%= event.name %></a>
        <p><%= event.description %></p>
        <a href="/admin/<%= event.slug %>">Uppfæra viðburð</a>
      </li>
    <% }) %>
  </ul>
<% } %>
<%- include('make-event') -%>
<div>
  <p>Innskráður sem <span>admin</span></p>
  <a href="/">Útskrá</a>
</div>

<%- include('footer') -%>
